meta {
  name: Delete a Budget
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/users/:userId/budgets/:budgetId
  body: none
  auth: bearer
}

params:path {
  userId: 
  budgetId: 
}

auth:bearer {
  token: {{token}}
}
